language: bash
services: docker
before_install:
  - cd 05\ RestWithSpringBootUdemy\ Docker/
  - echo "We are in the docker folder!"
script:
  - docker-compose build
before_deploy:
  - echo "${DOCKER_PASSWORD}" | docker login --username "${DOCKER_USERNAME}" --password-stdin
deploy:
  provider:script
  script:
    docker push iurilima/mysql-rest-with-spring-boot-udemy;
    docker push iurilima/rest-with-spring-boot-udemy;
  on:
    branch: master
notifications:
  email: false